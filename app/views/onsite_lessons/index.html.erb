<h1>Listing onsite_lessons</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Price in pennies</th>
    <th>Down payment price in pennies</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @onsite_lessons.each do |onsite_lesson| %>
  <tr>
    <td><%= onsite_lesson.title %></td>
    <td><%= onsite_lesson.description %></td>
    <td><%= onsite_lesson.price_in_pennies %></td>
    <td><%= onsite_lesson.down_payment_price_in_pennies %></td>
    <td><%= link_to 'Show', onsite_lesson %></td>
    <td><%= link_to 'Edit', edit_onsite_lesson_path(onsite_lesson) %></td>
    <td><%= link_to 'Destroy', onsite_lesson, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Onsite lesson', new_onsite_lesson_path %>
